export class addQuestion {
  public arr = [];
  public level;
//   public fetchArrayandLevel(questionArr, difficultyLevel) {
//     this.arr = questionArr;
//     this.level = difficultyLevel;
//   }
//   public getRandomInt(min, max) {
//     min = Math.ceil(min);
//     max = Math.floor(max);
//     return Math.floor(Math.random() * (max - min + 1)) + min;
//   }
}
